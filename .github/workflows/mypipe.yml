name: CI-pipeline

on:
  push:
    branches:
    - main
    
jobs:
  
  build:
    runs-on: ubuntu-latest
      
    steps:
    - name: helloworld
      run: echo "$RUNNER_OS"
      
    - uses: actions/checkout@v3
      with:
        repository: 'hakobmkoyan772/initial'
        path: './saml'
        
    - name: ls_current_path
      run: ls .
